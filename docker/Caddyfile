:80 {
    root * /var/www/public
    encode gzip

    # Route all requests through index.php when the file doesn't exist
    route {
        try_files {path} {path}/ /index.php?{query}
        php_fastcgi 127.0.0.1:9000
        file_server
    }
}
